<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_watchers"
            android:icon="@drawable/ic_watcher"
            android:title="@string/watchers" />
        <item
            android:id="@+id/nav_trackers"
            android:icon="@drawable/ic_runner"
            android:title="@string/trackers" />
    </group>

    <item android:title="@string/session">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_exit_to_app"
                android:title="@string/sign_out" />
        </menu>
    </item>

</menu>
